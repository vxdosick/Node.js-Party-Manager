<!DOCTYPE html>
<html lang="en">
<%- include("partials/head", {title: "Party Manager Edit"}) %>
<body>
    <div class="wrapper">
        <%- include("partials/header") %>
        <main>
            <div class="main__item main__container">
                <form class="main__form" action="/edit-party/<%= index %>" method="POST">
                    <div class="main__left">
                        <label for="name">Enter the meeting's title</label>
                        <input name="name" type="text" id="name" value="<%= party.name %>" required>
                        <label for="info">Enter the meeting information</label>
                        <textarea name="info" id="info" required><%= party.info %></textarea>
                    </div>
                    <div class="main__right">
                        <label for="date">Enter the date of the appointment</label>
                        <input name="date" type="datetime-local" id="date" value="<%= party.date %>" required>
                        <label for="loc">Location</label>
                        <input name="location" type="text" value="<%= party.location %>" required>
                        <label for="users">Enter the participants of the meeting</label>
                        <input name="users" type="number" value="<%= party.users %>" required>
                        <button class="button__general__settwo">Update</button>
                    </div>
                </form>
            </div>
        </main>
        <%- include("partials/footer") %>
    </div>
</body>
</html>